const conta   = Number(prompt("Insira o valor da conta: "))
const cliente = Number(prompt("Insira a quantidade de clientes: ")) 
const valor = (conta / cliente)

alert(`Valor por cliente: R$${valor}`)